machine:
  services:
    - docker
dependencies:
  override:
    - heroku plugins:install heroku-container-registry
test:
  override:
    - "true"
deployment:
  # release:
  #   tag: /v[0-9]+(\.[0-9]+)*/
  #   commands:
  #     - docker login --email=_ --username=_ --password=$HEROKU_TOKEN registry.heroku.com
  #     - heroku container:push web -a feedbackfruits-cayley
  staging:
    branch: master
    commands:
      - docker login --email=_ --username=_ --password=$HEROKU_TOKEN registry.heroku.com
      - heroku container:push web -a feedbackfruits-knowledge-graph
